# HPA - Horizontal Pod Autoscaler

An Apache deployment is running in the `autoscale` namespace. The metrics-server is already installed and configured.

Your task is to create a HorizontalPodAutoscaler that scales the deployment based on CPU usage.

> The lab environment is being prepared... wait until the prompt is ready.
