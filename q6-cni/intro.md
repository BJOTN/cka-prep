# CNI - Install a Container Network Interface

The cluster is running but has no CNI plugin installed. Pods cannot communicate with each other until a CNI is configured.

Your task is to choose and install a CNI plugin that meets specific requirements.

> No lab setup required — the cluster is ready to use.
