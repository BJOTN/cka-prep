# NetworkPolicy - Choose the Least Permissive Policy

Two deployments are running in separate namespaces:
- `frontend-deployment` in the `frontend` namespace
- `backend-deployment` in the `backend` namespace

Three NetworkPolicy YAML files have been prepared in `/root/network-policies/`. Only one of them allows frontend-backend communication in the least permissive way.

> The lab environment is being prepared... wait until the prompt is ready.
