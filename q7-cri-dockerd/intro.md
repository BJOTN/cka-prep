# CRI-Dockerd - Install and Configure

The `cri-dockerd` Debian package has been downloaded to `/root/cri-dockerd.deb`.

Your task is to install it, enable the service, and configure the required kernel network parameters to make the node Kubernetes-ready.

> The lab environment is being prepared... wait until the prompt is ready.
