# StorageClass - Create and Set as Default

The cluster may already have a default StorageClass. Your task is to create a new one with specific settings and make it the only default, without modifying any existing Deployments or PVCs.

> No lab setup required — the cluster is ready to use.
