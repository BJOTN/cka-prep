# Resource Allocation - WordPress Deployment

A WordPress deployment is running with 3 replicas and an init container.

Without proper resource limits, pods may consume too many resources and destabilize the node. Your task is to configure fair resource requests and limits for all containers.

> The lab environment is being prepared... wait until the prompt is ready.
