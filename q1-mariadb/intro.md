# MariaDB - Persistent Volume Recovery

A user accidentally deleted the MariaDB Deployment in the `mariadb` namespace.

The deployment was configured with persistent storage. Your task is to re-establish the deployment while ensuring data is preserved by reusing the available PersistentVolume.

> The lab environment is being prepared... wait until the prompt is ready.
