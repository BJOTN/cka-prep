# Ingress - Expose a Deployment via HTTP

An echo server deployment is running in the `echo-sound` namespace. It has no service and no Ingress yet.

Your task is to expose it with a NodePort service and then create an Ingress resource to route HTTP traffic to it via a hostname and path.

> The lab environment is being prepared... wait until the prompt is ready.
