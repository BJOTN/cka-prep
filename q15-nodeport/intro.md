# NodePort - Expose a Deployment

A deployment named `nodeport-deployment` is running in the `relative` namespace. It currently has no container port configured and no service.

Your task is to add the container port and expose it externally via a NodePort service.

> The lab environment is being prepared... wait until the prompt is ready.
