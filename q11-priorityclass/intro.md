# PriorityClass - Assign Scheduling Priority

A deployment named `busybox-logger` is running in the `priority` namespace. The cluster already has at least one user-defined PriorityClass.

Your task is to create a new PriorityClass just below the current highest value, then assign it to the existing deployment.

> The lab environment is being prepared... wait until the prompt is ready.
