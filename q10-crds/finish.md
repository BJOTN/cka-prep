# Well done!

You successfully explored cert-manager CRDs using kubectl.

Key points to remember:
- `kubectl get crd` lists all CRDs installed in the cluster
- `kubectl explain <resource>.<path>` shows inline API documentation for any field
- CRDs extend the Kubernetes API — they behave like native resources once installed
