# CRDs - Explore cert-manager Custom Resources

cert-manager is installed in this cluster. It registers several Custom Resource Definitions (CRDs) that extend the Kubernetes API.

Your task is to list those CRDs and extract documentation for a specific field using `kubectl explain`.

> The lab environment is being prepared... wait until the prompt is ready.
